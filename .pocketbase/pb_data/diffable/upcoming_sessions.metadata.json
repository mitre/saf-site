{
    "name": "upcoming_sessions",
    "columns": [
        "class",
        "date",
        "id",
        "signup_url",
        "status"
    ],
    "schema": "CREATE TABLE `upcoming_sessions` (`class` TEXT DEFAULT '' NOT NULL, `date` TEXT DEFAULT '' NOT NULL, `id` TEXT PRIMARY KEY DEFAULT ('r'||lower(hex(randomblob(7)))) NOT NULL, `signup_url` TEXT DEFAULT '' NOT NULL, `status` TEXT DEFAULT '' NOT NULL)"
}
