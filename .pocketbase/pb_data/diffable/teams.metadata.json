{
    "name": "teams",
    "columns": [
        "description",
        "id",
        "name",
        "organization",
        "slug",
        "website",
        "logo"
    ],
    "schema": "CREATE TABLE \"teams\" (`description` TEXT DEFAULT '' NOT NULL, `id` TEXT PRIMARY KEY DEFAULT ('r'||lower(hex(randomblob(7)))) NOT NULL, `name` TEXT DEFAULT '' NOT NULL, `organization` TEXT DEFAULT '' NOT NULL, `slug` TEXT DEFAULT '' NOT NULL, `website` TEXT DEFAULT '' NOT NULL, \"logo\" TEXT DEFAULT '' NOT NULL)"
}
