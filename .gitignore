node_modules
docs/.vitepress/cache
docs/.vitepress/dist
dist
.nuxt
.data/
.claude
.beads/.bdui

# Database (SQLite binaries ignored, but diffable/ JSON will be tracked)
drizzle/
*.db
*.sqlite
*.pglite
.pglite/

# Pocketbase (track migrations, ignore binary and runtime data)
.pocketbase/pocketbase
.pocketbase/pb_data/

# TrailBase (rejected solution - keep out of git)
.trailbase/

# Environment
.env
.env.local
.env*.local
.beads/recovery-context.md
.playwright-mcp/

# YAML data quality fixer backups
*.bak.*
